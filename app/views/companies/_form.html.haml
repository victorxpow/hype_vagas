= simple_form_for @company do |f|
  = f.input :name
  = f.input :url
  = image_tag(url_for(@company.logo), class: 'logo-company') unless @company.new_record? && !@company.logo.nil?
  = f.input :logo
  = f.button :submit, class: 'btn btn-primary'